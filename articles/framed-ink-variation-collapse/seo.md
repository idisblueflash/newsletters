**URL Slug**
illustration-style-variation-collapse

**Meta Description（221字）**
我想要黑白插画风格的图，但也想要每次生图都有点不一样，好从多张里挑出最合适的一张。于是我用同样的提示词、同样的配置，只换种子数值，让 Z-Image-Turbo 和 Flux.2 Klein 各生成一批图，再交给 Claude 打分看多样性。结果两个模型在插画风格下都栽了同一个跟头：换种子跟没换差不多，姿势、构图看不出太大区别。照片风格下明明换个种子就换一只完全不同的狗。到底是提示词的问题，还是插画风格本身就在偷偷锁死构图？

**Meta Title（18字）**
插画风格生图，为什么换种子也区别不大
